# Pin Gradio to match Hugging Face Spaces Docker (installs gradio[oauth]==4.44.1).
# Do not pin a different Gradio major/minor here or the build will fail with ResolutionImpossible.
gradio==4.44.1
# Must match gradio 4.44.1 metadata exactly or API schema / client can break.
gradio-client==1.3.0
pandas>=2.0,<3.1
numpy>=1.26,<3
scikit-learn>=1.3,<1.9
catboost>=1.2.7,<1.3
joblib>=1.3,<1.6
plotly>=5.18,<7
# Gradio 4.44.x oauth imports HfFolder — removed in huggingface_hub 1.x; stay on 0.x.
huggingface_hub>=0.19.3,<1.0
audioop-lts>=0.2.1
streamlit==1.42.0